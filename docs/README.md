Use the following command to clone the doc of the project from course server:

``` bash
wget --mirror \
     --convert-links \
     --adjust-extension \
     --page-requisites \
     --no-parent \
     https://fa25.cs161.org/proj2/

```